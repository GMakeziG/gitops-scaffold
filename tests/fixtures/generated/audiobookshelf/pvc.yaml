# Generated by gitops-scaffold. Review before applying — see README.md.
# Source: bind mount ./audiobooks
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf-audiobooks
  namespace: "apps"
  labels:
    app.kubernetes.io/name: "audiobookshelf"
    app.kubernetes.io/part-of: "audiobookshelf"
    app.kubernetes.io/managed-by: "gitops-scaffold"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # REVIEW REQUIRED: storage size could not be inferred from the Compose
      # definition -- Compose has no size concept at all. Defaulted to
      # 1Gi; set the real capacity your workload needs.
      storage: "1Gi"
---
# Generated by gitops-scaffold. Review before applying — see README.md.
# Source: bind mount ./podcasts
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf-podcasts
  namespace: "apps"
  labels:
    app.kubernetes.io/name: "audiobookshelf"
    app.kubernetes.io/part-of: "audiobookshelf"
    app.kubernetes.io/managed-by: "gitops-scaffold"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # REVIEW REQUIRED: storage size could not be inferred from the Compose
      # definition -- Compose has no size concept at all. Defaulted to
      # 1Gi; set the real capacity your workload needs.
      storage: "1Gi"
---
# Generated by gitops-scaffold. Review before applying — see README.md.
# Source: bind mount ./config
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf-config
  namespace: "apps"
  labels:
    app.kubernetes.io/name: "audiobookshelf"
    app.kubernetes.io/part-of: "audiobookshelf"
    app.kubernetes.io/managed-by: "gitops-scaffold"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # REVIEW REQUIRED: storage size could not be inferred from the Compose
      # definition -- Compose has no size concept at all. Defaulted to
      # 1Gi; set the real capacity your workload needs.
      storage: "1Gi"
---
# Generated by gitops-scaffold. Review before applying — see README.md.
# Source: bind mount ./metadata
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf-metadata
  namespace: "apps"
  labels:
    app.kubernetes.io/name: "audiobookshelf"
    app.kubernetes.io/part-of: "audiobookshelf"
    app.kubernetes.io/managed-by: "gitops-scaffold"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # REVIEW REQUIRED: storage size could not be inferred from the Compose
      # definition -- Compose has no size concept at all. Defaulted to
      # 1Gi; set the real capacity your workload needs.
      storage: "1Gi"
