services:
  app:
    image: myorg/app:1.0.0
    env_file:
      - .env
    environment:
      API_TOKEN: "sk_live_hardcoded_literal_value_12345"
      CLIENT_SECRET: "${CLIENT_SECRET}"
      ACCESS_KEY: ""
      PRIVATE_KEY:
      APP_ENV: production
