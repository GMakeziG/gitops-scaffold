{# ---------------------------------------------------------------------------
   Placeholder template for generators/kustomize/configmap.py.
   Variable contract is illustrative pending v0.2/v0.3 implementation.
--------------------------------------------------------------------------- #}
# Generated by gitops-scaffold. Review before applying — see VALIDATION_CHECKLIST.md.
# Only non-secret values are included here. See secret.example.yaml for
# credentials the analyzer flagged.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ service.name }}-config
  namespace: {{ namespace }}
  labels:
    app.kubernetes.io/name: {{ service.name }}
    app.kubernetes.io/part-of: {{ app.name }}
    app.kubernetes.io/managed-by: gitops-scaffold
data:
{% for var in config_vars %}
  {{ var.name }}: {{ var.value | tojson }}
{% else %}
  # TODO: no non-secret environment variables were detected for this service.
{% endfor %}
