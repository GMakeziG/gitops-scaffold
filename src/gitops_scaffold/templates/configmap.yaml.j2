{# Rendered by generators/kustomize/configmap.py. Only non-secret values ever
   reach this template -- secret-classified variables are excluded before
   rendering (see generators/secret_classification.py) and are documented in
   secret.example.yaml instead. #}
# Generated by gitops-scaffold. Review before applying — see README.md.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ resource_name }}
  namespace: {{ namespace | tojson }}
  labels:
{% for key, value in labels.items() %}
    {{ key }}: {{ value | tojson }}
{% endfor %}
data:
{% for var in config_vars %}
{% if var.requires_review %}
  # REVIEW REQUIRED: {{ var.review_reason }}
{% endif %}
  {{ var.name }}: {{ var.value | tojson }}
{% endfor %}
