{# ---------------------------------------------------------------------------
   Placeholder template for generators/kustomize/secret.py.
   This template must NEVER be rendered with real secret values. It only ever
   documents the keys the application expects, for operators to populate via
   their own secret manager (SealedSecrets, External Secrets Operator, SOPS).
--------------------------------------------------------------------------- #}
# Generated by gitops-scaffold.
# This is an EXAMPLE file, not a real Kubernetes Secret. Copy it, fill in real
# values out-of-band, and manage the result through your secret manager of
# choice. Never commit real values to this repository.
apiVersion: v1
kind: Secret
metadata:
  name: {{ service.name }}-secret
  namespace: {{ namespace }}
type: Opaque
stringData:
{% for var in secret_vars %}
  {{ var.name }}: "CHANGEME" # TODO: populate via your secret manager, not in git.
{% else %}
  # No secret-like environment variables were detected for this service.
{% endfor %}
