{# Rendered by generators/kustomize/kustomization.py. Never lists
   secret.example.yaml, README.md, or generation-report.json -- see
   docs/generation.md. In multi-service mode, the root kustomization lists
   service subdirectories (themselves containing their own kustomization.yaml)
   instead of files directly. #}
# Generated by gitops-scaffold. Apply with `kubectl apply -k .` or via
# FluxCD's Kustomization controller.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
{% if namespace %}
namespace: {{ namespace | tojson }}
{% endif %}
resources:
{% for resource in resources %}
  - {{ resource }}
{% endfor %}
