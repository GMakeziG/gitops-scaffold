{# ---------------------------------------------------------------------------
   Placeholder template for generators/kustomize/checklist.py.
   Variable contract is illustrative pending v0.2/v0.3 implementation.
--------------------------------------------------------------------------- #}
# Validation checklist for {{ app.name }}

Generated with **{{ analysis.confidence_percent }}% confidence**. Resolve every
item below before applying these manifests to a cluster.

{% for finding in analysis.findings %}
- [ ] **{{ finding.severity.value | upper }}**: {{ finding.message }}
{% if finding.remediation %}
      _Remediation: {{ finding.remediation }}_
{% endif %}
{% else %}
- [x] No findings were reported.
{% endfor %}

## Before you apply

- [ ] Replace every value in `secret.example.yaml` and manage it through your
      secret manager — never commit real secrets to this repository.
- [ ] Review every `TODO` / `REVIEW REQUIRED` marker in the generated YAML.
- [ ] Confirm resource requests/limits are appropriate for your workload.
- [ ] Run `gitops-scaffold validate <this-directory>` again after edits.
