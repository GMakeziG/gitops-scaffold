{# ---------------------------------------------------------------------------
   Placeholder template for generators/kustomize/pvc.py.
   Variable contract is illustrative pending v0.2/v0.3 implementation.
--------------------------------------------------------------------------- #}
# Generated by gitops-scaffold. Review before applying — see VALIDATION_CHECKLIST.md.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ service.name }}-{{ volume.target | replace("/", "-") | trim("-") }}
  namespace: {{ namespace }}
  labels:
    app.kubernetes.io/name: {{ service.name }}
    app.kubernetes.io/part-of: {{ app.name }}
    app.kubernetes.io/managed-by: gitops-scaffold
spec:
  accessModes:
    - ReadWriteOnce
{% if storage_class %}
  storageClassName: {{ storage_class }}
{% else %}
  # TODO: no default storage class was configured. Set storageClassName explicitly.
{% endif %}
  resources:
    requests:
      # REVIEW REQUIRED: storage size could not be inferred from the source definition.
      storage: 1Gi
