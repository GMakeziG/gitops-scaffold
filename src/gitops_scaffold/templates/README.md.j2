{# ---------------------------------------------------------------------------
   Placeholder template for generators/kustomize/readme.py.
   Variable contract is illustrative pending v0.2/v0.3 implementation.
--------------------------------------------------------------------------- #}
# {{ app.name }} — GitOps manifests

Generated by [gitops-scaffold](https://github.com/06ninjatronics/gitops-scaffold)
from `{{ source_path }}` on {{ generated_at }}.

**Confidence: {{ analysis.confidence_percent }}%** — see `VALIDATION_CHECKLIST.md`
before applying these manifests to a cluster.

## Contents

{% for file in generated_files %}
- `{{ file }}`
{% endfor %}

## Applying

This directory is laid out for FluxCD's Kustomization controller. To apply it
directly with `kubectl` instead:

```sh
kubectl apply -k .
```

## Secrets

`secret.example.yaml` is a placeholder, not a real Kubernetes Secret. Populate
real values through your secret manager of choice before deploying.
